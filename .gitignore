# Python
__pycache__/
*.py[cod]
.venv/
.venv-picaso/
.pytest_cache/
.ruff_cache/

# Pipeline caches (regenerable)
data/cache/

# The data artifact lives in GitHub Releases, not git (a 6k build is ~90 MB).
# data/RELEASE (committed) names the release tag; scripts/fetch_data.py downloads it.
data/planets.json

# Built static site (regenerable via `python -m web.build`; the Dockerfile builds it)
dist/

# Local Claude Code tooling / session data
.claude/

# Generated by `tools/exohub.py mprocs` — machine-specific absolute paths
mprocs.yaml

# OS
.DS_Store

# Private working docs — strategy and the internal defect list. Kept on disk, never published:
# this repo is public, and the defect list in particular discusses unresolved licensing and
# unshipped fixes, which is not something to broadcast while it is still true.
docs/marketing/
docs/DEFECTS.md
